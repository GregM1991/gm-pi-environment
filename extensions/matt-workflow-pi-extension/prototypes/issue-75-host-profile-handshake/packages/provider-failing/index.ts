import { createPrototypeProvider } from "../../provider";

export default createPrototypeProvider({
	providerId: "prototype-provider-failing",
	packageId: "issue-75-prototype-provider-failing",
	mode: "failing",
});

import { createPrototypeProvider } from "../../provider";

export default createPrototypeProvider({
	providerId: "prototype-provider-malformed",
	packageId: "issue-75-prototype-provider-malformed",
	mode: "malformed",
});
